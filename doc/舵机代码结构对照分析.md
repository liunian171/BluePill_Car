<!-- 编码: UTF-8 -->
# 舵机代码结构对照分析（框架合规审查）

> 创建: 2026-09-11。定位：**对照《代码风格与模块衔接指南》审查舵机链——哪些符合框架、哪些不符合**。
> 这是 `doc/结构优化顺序分析.md` §8.2 既定顺序的第 1 步（"舵机侧对照分析，用三问拆 main.c 的 sv_lim/servo_apply"）。
> 审查范围：`servo.h` / `servo.cpp` / `servo_protocol.h` / `servo_protocol.cpp` / `servo_bridge.h` / `servo_bridge.cpp` / `main.c` 舵机命令域。
> 依据条款：指南 §1 分层性质、§2 命名注释、§3 机制规则、§4 衔接义务 8 项；评审文档 C1-C8 / D1-D3 / §3.1 组件评估。
> **本文档只做分析，不改代码**（学习思考期约定，实施待立项）。

---

## 0. 一句话结论

> **本节为审查时的快照（2026-09-11 上午，整改前）**；整改后的现状见 §8「重构后结构快照」——原 4 项偏差中 S1/S2/S3/S4/S9/S10 已处理，仅桥接层遗留。

| 层 | 文件 | 评级 | 一句话 |
|----|------|:---:|--------|
| 桥接层 | `servo_bridge.h/.cpp` | ✅ 良 | 静态池 + placement new + id 检查 + 数组化四件套齐全，四桥中最标准；缺 handle 校验与错误通道 |
| 器件层 | `servo_protocol.h/.cpp` | ✅ 良 | 脉宽映射职责单一、注释含量化依据；但头文件把 CubeMX 依赖带进 TU，标定字段裸露 |
| 执行对象层 | `servo.h/.cpp` | ⚠️ 偏差 | **职责清单里写的"限位钳位"实现里没有**；域语义（物理角 0~270）与 main.c（输出域 -115~-65）双轨 |
| 组装层 | `main.c` 舵机命令域 | ⚠️ 错层 | 转向状态机（5 变量 + 2 函数）锁死在 main，本属**执行组件（转向）** |
| 文档形态 | `servo.cpp` 头部 1~172 行 | ⚠️ 严重陈旧 | 大段"待实现"残骸描述不存在的文件与旧架构，与同文件实现（175~199 行）矛盾 |

**核心判断（整改前）：舵机的"骨架"是四桥里最规范的，问题集中在"肌肉长错位置"——器件翻译对了，但钳位/域换算/标定状态机这三件执行栈的事，一半漏在执行对象层、一半漏在组装层。**


---

## 1. 符合框架的部分（应保持，勿在改造中破坏）

| # | 项 | 依据条款 | 现状证据 |
|:--:|----|---------|---------|
| G1 | 静态池 + placement new，零堆分配 | §3.3（范本） | `servo_bridge.cpp`：`pwm_protoc_pool` / `servo_pool` 双池 + 两次 placement new |
| G2 | 同构资源数组化（数组 + id） | §1.2 🔶 | `servo[MAX_SERVOS]` / `pwm_servo_protoc[MAX_SERVOS]` + id 索引，无分立变量 |
| G3 | 多态选择规则正确 | §1.2 🔶 | 协议多态 = C++ 抽象接口 `IServoProtocol` 注入；平台多态 = pwm ops 表。两类多态用对了场合，无交叉误用 |
| G4 | 句柄纪律 | §3.1 | bridge API 用 `void *handle`，无 HAL 句柄裸露在跨平台头文件 |
| G5 | 层边界正确（关键） | §1.1 | `main.c` 只 include `servo_bridge.h`，**不** include `servo.h`；C 世界不碰 C++ 类，走 `extern "C"` 门面 |
| G6 | 执行对象层持抽象接口引用 | §3.2 | `Servo` 成员 `IServoProtocol& protocol`（引用注入，构造后不可空，符合"引用而非指针"约定） |
| G7 | 器件层职责单一 | §1.2 器件层 | 脉宽映射只在 `PWMServoProtocol` 内；`set_position` 注释写明"0E3 → µs"与量化依据（0.27°/步）与为何改走 `pwm_set_pulse_us` |
| G8 | 命名合规 | §2.1 | 类 `Servo`/`PWMServoProtocol` PascalCase；函数 `servo_bridge_set_angle` 模块前缀 + 小写下划线；`ServoProtocol_t` 带 `_t`；`MAX_SERVOS` 全大写 |
| G9 | 标定值具名 + 来源可溯 | §2.2 | `SERVO_LIM_L` / `SERVO_CENTER` 等宏 + 注释注明"SS2 实测标定 2026-09-09 手动探边"，无无名 magic number |
| G10 | 安全铁律落地 | §4.3 ② | 舵机回直行位在巡线 init 之前（`main.c` 初始化序），上电即回中位 |
| G11 | 应答回执义务 | 义务 3 | SV 系列命令均 `ack()` + `cmd_note()`，双向确认齐备 |

---

## 2. 不符合框架的部分（按严重度排序）

### S1【高】`servo.cpp` 头部 1~172 行是设计期残骸，与实现互相矛盾

| 项 | 内容 |
|----|------|
| 位置 | `servo.cpp:1-172`（占全文 86%），描述内容为 `servo_core.cpp/.h`、`ServoUART`、`ServoI2C`、PCA9685、`app/main.cpp`、`STM32F4xx_hal_uart.h`、旧命名"功能层/适配层"，并含两段完整"待实现"伪代码 |
| **溯源证据（已核实）** | 本段与**上游驱动工程** `A_board_pwm_driver_test/Core/Src/driver/servo.cpp` 头部**字节级同源**——剥离前两文件全文**仅差 1 行**（上游 `max_angle(180)` → 本仓库 `max_angle(270)`，本项目换 270° 舵机），对应上游 `Servo_Driver_Design.md`。属上游（A 板 / STM32F427 时期）的设计草案随移植整文件复制；文中提到的 5 个文件/类在**上游与本仓库均不存在** |
| 违反 | §2.2 文件头规范（应写：ASCII 分层图 + **本文件在哪一层** + 换平台/换器件动不动这个文件）。现状写了，但写的是**已废弃的架构**，且与同文件 175 行起的真实实现矛盾 |
| 后果 | 新读者/新 Agent 会按头部去改 `servo_core.cpp`（不存在）、去接 `ServoUART`（未实现）；同一文件给出两套分层答案，直接破坏"对号入座选层"的可操作性 |
| 建议 | 删除 1~172 行，改按 §2.2 重写文件头：执行对象层定位 + 依赖白名单（只依赖 `IServoProtocol`）+ "换器件不动本文件"声明 |
| **✅ 处理结果（2026-09-11）** | 1~172 行**原样剥离归档**至 `doc/舵机旧设计稿_归档_上游A板.md`（含溯源说明，只读不维护）；`servo.cpp` 199 → 62 行，文件头按 §2.2 重写（层位图 + 职责 + 依赖白名单 + 可移植性 + 内嵌 S2/S3/S10 偏差标记 + 历史溯源）。Release 编译通过，**Flash 42016B / RAM 4096B 与改动前完全一致**（证实纯注释改动） |

### S2【高】执行对象层职责不完整：声明的"钳位"在实现里不存在

| 项 | 内容 |
|----|------|
| 位置 | `servo.cpp` 头部自述职责含"限位钳位"，但 `Servo::set_angle`（181~185 行）直接 `rate = (angle-min)/(max-min)*1000`，**无任何 clamp** |
| 违反 | §1.2 执行对象层定义明确含"语义翻译：**钳位**/死区/角度→脉宽"；指南 §1 要求"器件级目标"由本层收敛 |
| 后果 | 绕过 bridge 的任何调用（含未来转向组件）传入越界角即输出越界脉宽 → 舵机堵转（安全铁律 §0.2 落空） |
| 建议 | 钳位职责二选一：① 归 servo 内部（`Servo` 自持限位并 clamp，职责自洽）；② 明确归上层转向组件（则须删除头部"钳位"自述）。当前是"两处都声明、两处各做一半"（main.c `servo_set` + `servo_apply` 重复 clamp 两次） |

### S3【高】双坐标域并存且未声明，域换算硬编码在组装层

| 项 | 内容 |
|----|------|
| 位置 | `main.c:147-155` 用**输出域绝对角** [-115, -65]；`Servo` 用**物理角** [0, 270]（`servo.cpp:177`）；换算 `SERVO_RAW_CENTER(135) + a` 硬编码在 `main.c:164` |
| 违反 | 义务 8「单位与坐标域声明：跨模块接口必须在头文件声明单位与坐标域」。`servo.h` / `servo_bridge.h` 均无任何域声明 |
| 后果 | 本项目**已经因此出过事故**（SV -90 被双算成 -180 → 满舵，迫使 §3 修订命令域）。当前隐患仍在：任何新调用方都猜不出 `servo_bridge_set_angle` 吃的是物理角还是输出域角 |
| 建议 | 定域边界（见 §6 待决策）：推荐 **bridge 接口声明为"输出域绝对角"**（对外语义统一、与命令域/文档一致），`+135` 与 clamp 下沉到 servo 内部；届时 `servo_bridge_set_angle(id, -90)` 即直行，语义自明 |

### S4【高】舵机命令域状态机锁死在组装层（评审 C3 在舵机侧的具体表现）

| 项 | 内容 |
|----|------|
| 位置 | `main.c:151-174`：`sv_lim_l` / `sv_lim_r` / `sv_center` / `sv_cmd` / `sv_out` 五个 static + `servo_apply()` / `servo_set()` 两个函数；另散落于 244-246（init/上电回中）、285（二进制 0x10）、312（STOP 回正）、415-436（TXTCMD 分发） |
| 违反 | §1.2 层归属判据：组装层 = "创建实例 + 接线 + 决定谁什么时候跑"；本段做的是**钳位 + 标定限位 + 域换算 + 状态保持**，属**执行组件（运动意图 → 器件级目标）**。同时违反 §2.1（文件级 static 必须 `g_` 前缀） |
| 后果 | ① 无法 PC 桩测试（义务 7 落空）；② T1 转向模型无处可放，只能在 main 继续堆（评审 C3 预判的"膨胀温床"）；③ 标定值（§1.1）与状态耦合在 main，`car_config` 收敛更难 |
| 建议 | 抽 `steering` 执行组件（纯 C，`init(base/limits/center)` + `set(deg)` + `stop()` + 可选 `get()`），状态收进组件单实例；`main.c` 只留"init + 命令转发 + 应答"，即 §4.3 动刀位置 ②③④ |

### S5【中】`SERVO_PROTOCOL_UART` 空壳静默跳过

| 项 | 内容 |
|----|------|
| 位置 | `servo_bridge.h:11` 定义枚举值；`servo_bridge.cpp:21-22` `default: break;` |
| 违反 | 义务 6「非法输入不允许无声 return，至少走一次错误通道」 |
| 后果 | 传 `SERVO_PROTOCOL_UART` 时"初始化成功但什么都没有"——典型静默失败，排查成本高（评审 §3.1 已记"UART 空壳分支") |
| 建议 | 二选一：删除该枚举值（未实现不留接口，符合"预留必须注明启用条件否则删除"），或补实现并在 default 走错误通道 |

### S6【中】`servo_bridge_init` 缺 handle 合法性检查

| 项 | 内容 |
|----|------|
| 位置 | `servo_bridge.cpp:12-24`：查了 `id`，未查 `handle`；`*(PWM_Handle*)handle` 对 `NULL` 解引用 |
| 违反 | 义务 6（init 必须查参数合法性）。对比：`id` 检查做了，`handle` 漏了——同一函数内标准不统一 |
| 后果 | `handle == NULL` 时得到野引用并构造出"看似正常"的对象，故障推迟到首次 set 时爆发，定位困难 |
| 建议 | init 首行统一校验 `id` + `handle`（+ 协议枚举），非法即走错误通道返回 |

### S7【中】桥接层 TU 被 CubeMX 头污染（C6 的连带面）

| 项 | 内容 |
|----|------|
| 位置 | `servo_protocol.h:4` include `pwm.h` → `pwm.h:5` include `tim.h`（CubeMX）。而 `servo_bridge.cpp:2` include `servo_protocol.h` |
| **事实核查（读码确认）** | `pwm.h` **通篇不使用任何 `TIM_` 类型**——`PWM_Handle` 里句柄是 `void *htim`、通道是 `uint32_t channel`，函数指针也全是 `void *htim`（跨平台设计是对的）。它 include `tim.h` 的**唯一原因**是 `TIM_CHANNEL_x` 这类宏，而该宏的**唯一使用者是平台侧文件 `pwm_instance.c`**。即：为一个"实例文件"的需要，把策略层头文件绑架成了 HAL 依赖 |
| 违反 | §1.2 跨平台承诺："为兑现，**头文件也不得 include CubeMX 头**"（C6 已把 pwm.h 记为 ⚠️ 反例） |
| 后果 | ① 换芯片时 `pwm.c` 可零修改，但 `pwm.h` 要动；② **污染扩散**：`servo_protocol.h`（器件层）→ `servo_bridge.cpp`（桥接层）——一个本该"只做 C 门面、与芯片无关"的编译单元里，因此出现了 CubeMX 宏；③ HAL 头携带大量宏与宏函数（`__HAL_TIM_xxx` 等），被包含后换芯片时容易以"编译错误遍地"的形式爆发 |
| 建议 | 🎯 **比"下沉"更简单——直接删除 `pwm.h:5` 即可**（见下方实验）。`TIM_CHANNEL_x` 的真正使用者 `pwm_instance.c` **自己已经 include 了 `tim.h`**（第 10 行），`pwm_platform_ops.c` 也自己 include（第 2 行） |

**实验与实测数据（2026-09-11，读码 + 编译验证）**

| # | 证据 | 数据 |
|:--:|------|------|
| E1 | `TIM_CHANNEL_x` 全仓库真实使用点 | 仅 `pwm_instance.c:16/25/34`（3 处 `.Channel = TIM_CHANNEL_x`）＋ `encoder_platform_ops.c` 用 `TIM_CHANNEL_ALL`（自带 include）＋ `tim.c`（CubeMX 自身）。**`pwm.h` 里一处都没用到**（只有第 132 行注释里出现过名字） |
| E2 | 谁已经自己 include `tim.h` | `pwm_instance.c:10` ✅、`pwm_platform_ops.c:2` ✅ → **`pwm.h` 那行属纯多余残留** |
| E3 | 历史由来 | `pwm.c:4` 留着 TODO：「仅因下方实例定义引用了 `htim5` / `TIM_CHANNEL_4`」——实例定义原先放在 `pwm.c` 末尾，后分离到 `pwm_instance.c`，`pwm.h` 的 include 是那时期留下的 |
| E4 | **删除实验** | 临时删掉 `pwm.h:5` 后 `cmake --build build/Release` **10/10 编译+链接全部通过**，Flash 42016B / RAM 4096B **完全不变**；随后已完整恢复（`diff` 无差异，git 状态干净） |
| E5 | 污染规模实测（`gcc -H` 包含链） | 仅 `#include "pwm.h"` 一个文件，就拉入 **38 个头文件，其中 28 个来自 HAL/CMSIS**（hal → hal_conf → hal_rcc → hal_rcc_ex → hal_gpio → hal_gpio_ex → hal_tim → hal_tim_ex → hal_def …） |
| E6 | 受影响面 | 共 **8 个文件** include `pwm.h`：`motor_bridge.h`、`motor_protocol.h`、`pwm_platform_ops.h`、`servo_protocol.h`、`pwm.c`、`pwm_instance.c`、`uart_cmd_parser.c`、`main.c` → 这些 TU 每个都白吃 38 个头 |
| E7 | 附带发现的错误注释 | `pwm_platform_ops.c:15` 自称"本文件是整套驱动中唯一 `#include "tim.h"` 的地方"——**与事实不符**（`pwm.h`、`pwm_instance.c` 也在 include）。建议随 C6 一并修正 |

### S8【中】"有检查、无错误通道"——义务 6 只做了一半

| 项 | 内容 |
|----|------|
| 位置 | 四个 bridge 出口均 `if (id >= MAX_SERVOS \|\| servo[id] == nullptr) return;` |
| 违反 | 义务 6 后半句：非法输入"至少走一次错误通道（串口错误输出或返回码，二选一，模块内保持一致）"；且要求"模块内保持一致" |
| 后果 | "未初始化就调用"与"正常执行"对外部完全不可区分，无线调试时表现为"指令无响应"类玄学问题（评审已将其与 §5 堆分配惨案并列为同族） |
| 建议 | 家族契约统一决定错误通道形态（见 §6 待决策二：返回码 vs 错误日志），四个桥一致执行——该项属桥接层家族契约范畴，不宜舵机单独改 |

### S9【低】`servo.h` 缺文件头注释块 + include guard 用保留标识符

| 项 | 内容 |
|----|------|
| 位置 | `servo.h:1-4`：只有 `#ifndef __SERVO_H` + `#include <stdint.h>`，无分层图/层位/可移植性声明；guard 双下划线属实现保留命名空间 |
| 违反 | §2.2 文件头规范；`__SERVO_H` 是 C/C++ 标准保留标识符（同类：`__SERVO_PROTOCOL_H__`、`__SERVO_BRIDGE_H__`） |
| 建议 | 补文件头（层位 + 依赖白名单 + 换器件是否修改）；guard 改 `SERVO_H` / `SERVO_PROTOCOL_H` / `SERVO_BRIDGE_H` |

### S10【低】标定字段裸露 public，无注入入口

| 项 | 内容 |
|----|------|
| 位置 | `Servo::min_angle` / `max_angle`、`PWMServoProtocol::min_pulse` / `max_pulse` 均为 public 成员，无 setter，bridge 亦不暴露 |
| 违反 | §3.2 接口注入精神（参数应由构造/接口注入而非裸改字段）；评审 §3.1 记"min/max_pulse 未通过 bridge 暴露" |
| 后果 | 换舵机（如 180° 型、500~1500µs）必须改源码而非改配置；且 `Servo` 与 main 的限位语义重叠（评审 §3.1 ①） |
| 建议 | 随 §6 待决策一的域方案一并定：限位单一所有者 + 构造参数注入（或经 `car_config`） |

### S11【低】未验证项以感叹号注释留在头文件

| 项 | 内容 |
|----|------|
| 位置 | `servo_bridge.h:22`：`//!!!!!!!!!!该函数还未测试是否有用!!!!!!!!!!!!!` |
| 违反 | §5.5 状态标注约定：未验证项应在开发跟踪/调试总结登记，用规范标注（⬜/⚠️），而非头文件感叹号；且现在 `servo_bridge_stop` 已实车链路可用（STOP 走的是 `servo_set(center)` 而非 stop，故该注释既过期又误导） |
| 建议 | 删除感叹号注释；若 `stop()` 语义确未验证，在开发跟踪登记一条 ⬜ 条目 |

---

### 2.1 整改结果（2026-09-11，`feat(steering)` 批次，commit `aa7b70b`）

| 发现 | 处理结果 | 证据 |
|:--:|---------|------|
| S1 | ✅ 1~172 行原样归档至 `doc/舵机旧设计稿_归档_上游A板.md`，`servo.cpp` 按 §2.2 重写文件头（199→62 行） | commit `7262ec0` |
| S2 | ✅ 补**器件行程钳位**：`Servo::set_angle` 先钳 [0,270] 再换算；`PWMServoProtocol::set_position` 补显式脉宽钳位 [500,2500]µs（不再依赖 `map` 隐式端点行为） | `servo.cpp` / `servo_protocol.cpp` |
| S3 | ✅ 域边界定为**两层两域**：组件域=输出域绝对角、执行对象层及以下=物理角；`+135` 安装偏置归 `steering` 配置（车知识），`main.c` 的硬编码换算删除 | `steering.h` 域声明块 |
| S4 | ✅ 舵机命令域 7 符号 → 新建执行组件 `steering.c`（纯 C，零 HAL，输出函数指针注入）；`main.c` 只留配置表 + 转发 | `Core/Src/driver/steering.c` |
| S9 | ✅ `servo.h` 补文件头（层位/职责/域声明/可移植性）；guard 改 `SERVO_H`（同类：`SERVO_PROTOCOL_H`） | `servo.h` |
| S10 | ✅ 行程/脉宽极限改**构造注入**（`Servo(proto, min_angle, max_angle)`、`PWMServoProtocol(handle, min_pulse, max_pulse)`），字段转 private | `servo.h` / `servo_protocol.h` |
| S11 | ⬜ **待做**（`servo_bridge.h` 感叹号注释 + NULL handle 校验，属桥接层家族契约批次） | — |
| S5/S6/S8 | ⬜ **待做**（错误通道形态 / UART 空壳 / init 校验，属桥接层家族契约批次） | — |

> 组件侧新增 **PC 桩 `test/host_steering_test.c`：33/33 PASS**（含钳位、域换算、限位归一化、错误通道）；`txt_cmd` 桩回归 60/60 PASS。硬件在环（台架/实车）验证待用户接线后执行——**PC 通过 ≠ 硬件通过**（§5.5）。

---

## 3. 三问拆解：`main.c` 的 `sv_lim` / `servo_apply` 该去哪

> 对应 `结构优化顺序分析.md` §8.2 第 1 步的既定问题。

| 问 | 答 | 判据 |
|----|----|------|
| **① 属于哪一层？** | **执行组件（转向）**，不是组装层 | 它做"运动意图（输出域角）→ 器件级目标（物理角 + clamp）"的语义收敛，无实例创建、无节拍调度。组装层判据是"new/init 别的组件 + 决定谁什么时候跑"，本段两条都不占 |
| **② 实例/状态在哪？** | 5 个文件级 static 标量（`sv_lim_l/r`、`sv_center`、`sv_cmd`、`sv_out`）+ 2 个 static 函数，全在 `main.c`，且**无 `g_` 前缀** | §1.2 执行组件"输入意图与反馈物理量，输出器件级目标"；§2.1 static 命名规则 |
| **③ 与姿态（attitude）是否同构？** | **同构（可共用组件模板），但深度不同** | 同：都是"标定/管线状态长错位置"——姿态的 ~90 行校准+滤波调度在 `imu_bridge`（胖桥），舵机的钳位+域换算在 `main`。异：姿态在**感知栈（上行、多通道）** → 需通道实例结构体 + 数组；舵机在**执行栈（下行、单通道标量）** → 只需单实例结构体，**不需要数组化** |

**由此得出拆分模板（两项共用）**：注入器件接口 + tick 外部化 + 标定量集中 + PC 桩可编译 + 状态归组件实例。
**边界确认（重要）**：`servo_bridge`（桥）与 `PWMServoProtocol`（器件）**不在改造范围**——它们只需按家族契约补错误通道/校验/头文件，不需要搬家。要搬的只有 main.c 里那 7 个符号。

---

## 4. 钳位该在哪一层（S2 展开）

**结论：钳位不是一个，是两个，分属两层。** 用户提出的判断是对的——**钳位与设置角度是两件事**：
- **限位是配置态**（初始化时设置一次）
- **钳位是执行态的强制约束**（每次必经，且不可绕过）

把钳位写进 `set_angle` 的参数域、或交给调用方自觉执行，都会出错。现状恰好两头都错：限位在调用方（main.c），被调用方（Servo）裸奔。

| # | 钳位类型 | 边界值来源 | 应属层 | 理由 | 当前实现 |
|:--:|---------|-----------|--------|------|---------|
| 1 | **器件物理极限**<br>角度域 / 脉宽域 | 舵机手册：270° 行程 / 500~2500µs | **执行对象层 `Servo`** 管角度域 [0,270]；**器件层 `PWMServoProtocol`** 管脉宽域 [500,2500] | 这是"**器件知识**"，与整车标定无关，换车也必须存在；必须是最底层兜底，任何调用路径都绕不过 | ⚠️ 原有兜底是**隐式**的（靠 `map` 端点行为），**无显式表达、无报告**；2026-09-11 已补显式钳位（详 R1-R4） |
| 2 | **机构行程限位**<br>前轮可用行程 | 实车标定（SS2 手动探边）：输出域 [-115, -65] | **执行组件 `steering`** | 依赖标定值、依赖整车机构几何，属"**车知识**"；应由配置注入（`car_config`），换车只改配置不改代码 | ✅ 2026-09-11 归位 `steering`（唯一所有者），main.c 的 7 个符号已移除 |

**当前实现的精确核查（2026-09-11 读码确认 + 一处更正）**

| # | 结论 | 代码依据 |
|:--:|------|---------|
| R1 | `Servo::set_angle`（改前）**无 clamp**。**后果更正**：并**不会**产生越界脉宽（下游 `map` 端点钳位兜住），而是越界角被**静默饱和**到行程端点、无任何报告；极端输入下 `uint16_t rate` 截断使实际输出角不可预期（脉宽仍落在安全域内） | `servo.cpp`（改前）；`tool.c:map` |
| R2 | ❌ **原表述有误，此处更正**：本项目 `tool.c` 的 `map()` 是**整数版且自带端点钳位**（`x` 超界直接返回 `out_min`/`out_max`），故 `map(rate,0,1000,500,2500)` **不会**线性外推出 >2500µs —— 脉宽一直被**隐式**兜在 [500,2500]。真正的问题是"**安全依赖隐式副作用 + 越界无报告**"，而非"完全没有钳位"（上一版写的"线性外推"是浮点 map 的行为，与本项目实现不符） | `tool.c`：`if (x < in_min) return out_min;` … `else return out_max;` |
| R3 | 驱动层另有"**占空比 ≤ 100%**"的通用兜底（`pwm_set_pulse_us` 内钳到 `period_us`，50Hz → 20000µs）。语义与舵机极限无关，**不可作为舵机安全依据**（两者相差 8 倍） | `pwm.c:145` |
| R4 | `pwm_set_duty_0E3` **完全无钳位**（duty 传 2000 → CCR = 2×ARR，硬件表现恒高）。舵机现走 `pulse_us` 路径，此项主要影响电机链 | `pwm.c:121-128` |

> **准确表述（更正后）**：舵机安全过去一直被**隐式**保证——靠 `map` 的端点行为，而不是显式钳位。问题在于**读代码看不出有保护、越界也无报告**（义务 6 静默失败）。2026-09-11 已补**显式**钳位：`Servo` 管角度域（[0,270]，器件行程）、`PWMServoProtocol` 管脉宽域（[500,2500]µs），并加配置合法性拒绝输出。补在器件侧是正确的——500~2500µs 是**这只舵机**的知识，写进通用 pwm 驱动会破坏其通用性；`Servo` 侧钳位保证任何调用路径都绕不过。

**两道钳位的大白话对照（用户理解确认 ✅）**

| | **器件极限**（硬极限 — 保护舵机） | **应用行程**（软限位 — 保护机构） |
|---|---|---|
| 归层 | 执行对象层 `Servo`（角度域）／器件层 `PWMServoProtocol`（脉宽域） | 执行组件 `steering` |
| 数值 | **0~270°** ／ 脉宽 **500~2500µs** | 输出域 **[-115, -65]** ↔ 物理角 **20~70°** ／ 脉宽 **648~1019µs** |
| 性质 | 舵机自身的物理边界，**换车也不变** | 本车转向连杆的可用区间，**标定得来、换车就变** |
| 宽度 | 270°（全行程） | 50°（仅占全行程 18.5%） |
| 定位 | **兜底**——正常路径不应碰到 | 正常路径的**第一道**，实际应用只用这一段 |
| 知识类型 | 器件知识 | 车知识（应进 `car_config`） |

> **两道的关系**：应用行程（50°）比器件极限（270°）窄得多，所以正常路径上先是组件拦；只有当有人**绕过组件**（临时调试代码、未来巡线 corr→转向融合直调 bridge、PC 桩）时，`Servo` 的器件极限才兜底——这才是"纵深防御"的实际含义，也解释了为什么两道都要留。

**为什么"器件极限钳位"归执行对象层（四条判据）**

| # | 判据 | 说明 |
|:--:|------|------|
| 1 | **信息同源** | `Servo` 已持有角度域两端（`min_angle` / `max_angle`），钳位要用的正是这两端 + 它正在做的换算。把钳位搬走 = 同一份信息分居两处 → 必然"各算一半"（现状即如此） |
| 2 | **知识归属是"器件"而非"车"** | 0~270° 是舵机自身物理行程（手册数据），换车 / 换机构 / 改标定都不变 → 属**器件知识** → 归器件侧。同源道理：脉宽 500~2500µs 归器件层 `PWMServoProtocol` |
| 3 | **兜底必须"绕不过"** | `set_angle` 是通往器件的必经入口，在此钳位则任何调用方（`steering` 组件 / 临时调试代码 / 未来 PC 桩）都绕不过。若放 `steering`，只有走组件才受保护；若放 `pwm`，驱动不知道这是舵机（20000µs 对通用 PWM 完全合法），只能给出"周期上限"这类通用边界——正是 R3 那道对舵机无效的兜底 |
| 4 | **层定义本就如此 + 反向检验** | 指南 §1.2 对执行对象层的职责原文即"语义翻译：**钳位** / 死区 / RPM→千分比 / 角度→脉宽"。反向检验：若钳位不在本层，则 `min_angle` / `max_angle` 沦为无意义裸字段（现状），任何人直接经 bridge 调用即失去保护 |

**反过来：机构行程限位为什么不能放这一层** —— `[-115,-65]` 是标定值（**车知识**），`Servo` 不该知道"这台车的转向连杆能到哪"。放进来会导致"换车必须改 `servo.cpp`"，直接破坏"换整车零修改"的跨平台承诺。所以两道钳位边界值不同、语义不同，各归其层，不是重复。

**三条配套设计规则（建议）**

| # | 规则 | 说明 |
|:--:|------|------|
| 1 | **限位与钳位分离** | 限位是配置接口（`servo_set_limit(min,max)` / 转向组件配置），在 init 后设置一次；`set_angle` 只接收目标角，**不在参数里带限位** |
| 2 | **钳位放被调用方，不放调用方** | 钳位必须在被调用方内部强制执行，才能保证"任何路径绕不过"。现状反了：钳位在调用方 main.c,被调用方 Servo 无保护——绕过 bridge 直接调用即失去保护 |
| 3 | **两层各留一道，但语义不同** | 器件极限（层 1）与机构行程（层 2）是**两类**约束，各留一道是纵深防御，**不算重复**；重复指的是"同类约束被放在两处"（评审 §3.1 ① 指的正是这类——`Servo::min/max_angle` 与 main.c 的行程限位是同一类，属真重复） |

> 与 §6 待决策 2 的关系：「限位唯一所有者」指的是**机构行程限位**（同类只留一处）；器件物理极限兜底是**另一类**，不参与"唯一所有者"的选择。

---

## 5. 治理执行结果（契约先行 → 实现后搬）

| 序 | 动作 | 状态 | 证据 |
|:--:|------|:---:|------|
| 1 | 清 `servo.cpp` 陈旧头部块、补 `servo.h` 文件头、guard 改名 | ✅ S1/S9 完成；S11 待随家族契约批次 | `7262ec0`（S1 归档）/ `aa7b70b`（S9 guard+文件头） |
| 2 | **定域契约**：域边界与限位所有者（§6） | ✅ 已定案并落地 | §6 决策表；`steering.h` 域声明块 |
| 3 | 抽 `steering` 执行组件（钳位 + 域换算 + 标定限位 + 状态），main 只留转发；配套 PC 桩 | ✅ 完成（PC 桩 33/33）；**实车/台架回归待用户接线** | `aa7b70b`；`test/host_steering_test.c` |
| 4 | 家族契约统一：错误通道形态、init 签名、id 校验、handle 校验、协议枚举清理 | ⬜ 待立项（与 IMU 链合并成方案） | — |
| 5 | 策略层去 CubeMX 依赖（`pwm.h`/`pwm.c` 移除 `tim.h`） | ✅ 完成 | `ccfcda9`（C6） |
| 6 | 顺带：器件侧显式钳位（`Servo` 角度域 / `PWMServoProtocol` 脉宽域） | ✅ 完成 | `aa7b70b` |

> 顺序理由与"契约先行、实现后搬"一致：序 2 是契约（域声明/限位归属），先行于序 3 的搬迁——避免组件建在错的域上返工两次（本次即按此顺序，契约与实现同批但次序明确）。
> 附加收益：`pwm` 链与舵机链的包含链在序 5 后彻底摆脱 HAL 头（单 `pwm.h` 由 38 个头 → 纯 `stdint`）。

---

## 6. 决策结论（2026-09-11 已定，用户授权"看你"）

> 原为两个待决策点，现已定案并落地（commit `aa7b70b`）。此节保留决策依据，供回溯。

| # | 问题 | **决策** | 理由 |
|:--:|------|---------|------|
| 1 | 域边界定在哪一层？ | **域边界划在"组件 ↔ 执行对象层"之间**：组件对外=**输出域绝对角**（直行 -90）；执行对象层及以下=**物理角** [0,270]。`+135` 安装偏置归 `steering` 配置 | 原倾向"B：`+135` 下沉到 Servo"被推翻——**135 是安装偏置（车知识）**，塞进执行对象层会导致"换车/换舵机安装就必须改 servo.cpp"，破坏"换整车零修改"承诺。域换算的归属必须跟知识归属走 |
| 2 | 限位（clamp）唯一所有者？ | **机构行程限位归 `steering` 组件**（唯一所有者，输出域）；`Servo` 只保留**器件行程** [0,270]（器件知识） | 二者语义不同、边界值不同，不算重复；评审 §3.1 ① 指的"重复"是**同一类**限位被放两处（原 `Servo::min/max_angle` 与 main.c 行程限位，现已各归其位） |
| 3 | 组件实现方式（附带决策） | 纯 C + **输出函数指针注入**；零 HAL、零 `HAL_GetTick`、只依赖 `<stdint.h>` | 满足义务 7（PC 桩可编译）；组件不 include 桥接层头，执行栈不反向依赖（§1.2 决策组件接口面板"交界不拥有"） |
| 4 | 是否引入 tick 参数？ | **不引入** | 当前无超时/斜坡/周期逻辑；按"预留未用即删除"原则，等加转向斜坡或 T1 转向模型时再补 |

---

## 7. 与评审文档的对应关系

| 本文发现 | 对应评审条目 | 说明 |
|:--:|:--:|------|
| S4 | **C3**（高） | main.c 上帝模块——舵机命令域为最典型样本 |
| S7 | **C6**（低）+ §1.2 跨平台承诺 | pwm.h 拖 CubeMX 依赖，舵机链被连带 |
| S8 / S5 / S6 | **义务 6**（指南 🔶）+ §3.1 Servo ⚠️ | 静默失败禁令、UART 空壳、handle 未校验 |
| S2 / S10 | §3.1 Servo ⚠️ ① | "限位配置分裂两处、语义重叠" |
| S1 / S11 | 文档债同族（D2/D3 模式） | 陈旧自述/未验证标记停留在源码内 |
| §3 三问 | §8.2 步骤 1 / 步骤 2 输入 | 本分析即步骤 2"家族契约 + 组件拆分完整方案"的舵机侧输入 |

> 审查快照：基于 commit `88ba51c` 工作区。改造完成后逐项回写本表状态并同步 `开发跟踪.md`。

---

## 8. 重构后结构快照（2026-09-11 晚，含真机验证）

> 基线：commit `1de3630`。本节是**当前**结构权威描述；§0~§7 为审查过程与决策记录，保留供回溯。

### 8.1 分层与文件（8 个文件 / 580 行）

| 层 | 文件 | 行数 | 职责 | 状态 |
|----|------|:---:|------|:---:|
| 组装层 | `main.c`（舵机接线段） | — | `g_steering_cfg` 标定表注入 + `steering_output_to_servo` 输出绑定 + 命令转发 + 应答 | ✅ |
| **执行组件** | `steering.h` / `steering.c` | 93 / 166 | 机构行程钳位（输出域，唯一所有者）+ 域换算（+135 安装偏置）+ 状态保持；纯 C、零 HAL、输出函数指针注入 | ✅ 新建 |
| 执行对象层 | `servo.h` / `servo.cpp` | 69 / 79 | 物理角 → 千分比 + **器件行程钳位** [0,270]；行程参数构造注入 | ✅ 已补 |
| 器件层 | `servo_protocol.h` / `.cpp` | 46 / 54 | 千分比 → 脉宽 µs + **显式脉宽钳位** [500,2500]；整数运算（去 `map` 依赖） | ✅ 已补 |
| 桥接层 | `servo_bridge.h` / `.cpp` | 31 / 42 | C 门面：静态池 + `placement new` + id 检查 + 数组化 | ⚠️ 唯一遗留 |
| （驱动） | `pwm.c` / `pwm_instance.c` / `pwm_platform_ops.c` | — | 脉宽 → CCR → 寄存器；已去 CubeMX 头依赖（C6） | ✅ |

**数据流**：`main.c` 命令 → `steering_set(输出域角)` → ①行程钳位 ②`+135` 域换算 → `steering_output_fn` → `servo_bridge_set_angle(物理角)` → `Servo::set_angle` ③器件行程钳位 → `PWMServoProtocol::set_position` ④脉宽钳位 → `pwm_set_pulse_us` → CCR。
**新增文件后须重跑 `cmake -S . -B build/Release`**（GLOB 才收录新源文件）。

### 8.2 与"目标态"的差距（剩余项收敛到桥接层 + 自查项）

| # | 项 | 位置 | 性质 | 建议 |
|:--:|----|------|------|------|
| S11 | 头文件残留 `//!!!该函数还未测试是否有用!!!` | `servo_bridge.h:22` | 注释规范 | 删除（`stop()` 现由死代码 `uart_cmd_parser.c` 调用） |
| S6 | `init` 查了 `id` 未查 `handle` | `servo_bridge.cpp:12-24` | 义务 6 | 加 `handle == nullptr` 校验 |
| S8 | 四个出口"有检查、无错误通道" | `servo_bridge.cpp` | 义务 6 | **待家族契约统一错误通道形态**（返回码 vs 日志） |
| S5 | `SERVO_PROTOCOL_UART` 空壳静默 `return` | `servo_bridge.h:11` / `.cpp:21` | 义务 6 | 删除枚举值或补实现（家族契约批次） |
| N1 | `servo_bridge_stop()` 无活代码调用 | 仅 `uart_cmd_parser.c`（死代码 C1） | 死接口 | 随 C1 一起：收编 parser 或删该函数 |
| N2 | `Servo::get_angle()` 全工程零调用 | `servo.cpp` | ⚠️ **本轮新增的"预留未用"** | 要么用于 OLED/应答显示，要么删除（指南：预留须注明启用条件，否则删） |
| N3 | `steering_is_init()` / `steering_get_lim_min/max()` | `steering.c` | 仅 PC 桩使用 | 建议**用起来**：`SL/SR/SC` 的 `BAD` 应答可回显"实际生效限位"，既改善诊断又消除"预留未用" |

> 说明：S5/S6/S8 属**桥接层家族契约批次**（与 IMU 链 / oled 归一一起定形态），不宜单点改；N1~N3 是可在 10 分钟内清掉的自查项。

### 8.3 证据链现状

| 类别 | 状态 | 证据 |
|------|:---:|------|
| PC 桩 | ✅ | `test/host_steering_test.c` **33/33 PASS**；`txt_cmd` 回归 60/60 |
| 编译/资源 | ✅ | Release **Flash 42668B (65.11%)** / RAM 4176B (20.39%) |
| 真机命令链路 | ✅ | 12/12 符合预期（含钳位、限位即时生效、`BAD` 错误通道、二进制同源、无回归） |
| 示波器脉宽 | ⬜ | 833 / 1019 / 648µs 未实测（应答对 ≠ 脉宽对） |
| 舵机物理动作 | ⬜ | 回直行位 / 满舵需目视 |
| 参与闭环（巡线融合） | ⬜ | steering 目前**只受命令驱动**，未被巡线 `corr` 调用 |
