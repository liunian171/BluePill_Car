<!-- 编码: UTF-8 -->
# 双链路仲裁设计文档（USB CDC + UART 蓝牙）

> 状态：✅ **P1 / P2 / P3 三期全部收口（2026-09-19）** —— 链路并存 → 指挥权仲裁 → **开关收口 + README §3 双链路改版**
> 上游依据：USB CDC 独立链路真机验证 PASS（同日，COM17，PING/PONG 10/10，回显 20/20 零丢包）
> 关联文档：`README.md` §3（协议权威，本设计不改协议）/ `代码风格与模块衔接指南.md` §1（脑干模型）
> 决议记录：`开发跟踪.md` 设计决议 2026-09-19

---

## 1. 背景与目标

| 项 | 内容 |
|----|------|
| 决策 | **USB 与 BT04 串口双链路并存** + 指挥权仲裁（用户拍板 2026-09-19） |
| USB 链路 | CDC 虚拟串口（PA11/PA12，0483:5740 → COMx），ROS 上位机主链，带宽 100KB/s 级 |
| UART 链路 | USART2 + BT04（9600-8N1，COM 口随配对漂移），离场遥控备份链 |
| 协议 | **零修改**——README §3 文本命令 + 0xAA 帧体系 + ODOM/ATT 上行帧，纯字节流语义与物理载体无关 |
| 核心诉求 | USB 主导；USB 失联串口自动接管；串口可显式接管/让出；USB 可显式抢回 |

## 2. 指挥权状态机（link_arbiter 组件核心）

### 2.1 状态与转移

| 状态 | 含义 | 应答/遥测/ODOM 路由 |
|------|------|--------------------|
| OWNER_USB | USB 主导（默认） | USB 链路 |
| OWNER_UART | UART 接管 | UART 链路（BT04） |

| 转移 | 触发 | 类型 |
|------|------|------|
| USB → UART | owner 链路发 `LINK UART` | 显式接管 |
| UART → USB | owner 链路发 `LINK USB`（= 让出）或 USB 链路发 `LINK USB`（= 抢回） | 显式切换 |
| USB → UART | **自动 failover**：usb_alive 由真变假（§4 三判据任一） | 自动，广播 `USB LOST → UART TAKEOVER` |
| UART → USB | ~~自动~~ **无**——USB 恢复仅广播 `USB READY`，不自动抢回（决议见 §4.2） | 显式 LINK 命令 |

### 2.2 命令语义表（新增 LINK 命令，注册进 txt_cmd）

| 命令 | 来源链路 | 行为 | 应答 |
|------|---------|------|------|
| `LINK ?` | 任意 | 查询当前 owner | `OWNER:USB` / `OWNER:UART` |
| `LINK USB` | 任意 | 切到 OWNER_USB | `LINK:USB OK` |
| `LINK UART` | 任意 | 切到 OWNER_UART | `LINK:UART OK` |
| `STOP` | 任意 | 急停（**安全例外，永远生效**） | 现有 STOP 语义 |
| 其他全部命令 | 仅 owner | 执行 | 非 owner 回 `BUSY:<owner>` |

**安全铁律：STOP 权限高于仲裁**——刹车权不归指挥权管。任何链路、任何状态、任何会话期，STOP 直达急停。

### 2.3 会话锁（调参保护）

`STEP / TEL / REC / ODOM / ITEL` 任一激活期间，`LINK` 切换命令拒绝，回 `BUSY:SESSION`——防止遥测流被切走一半导致上位机解析断流。`STOP` 不受限（急停本来就关闭所有会话工具）。

## 3. 双 ringbuf 与消费契约（防阻塞设计）

### 3.1 拓扑

```
ISR 层:   USART2 RX 中断 → uart ringbuf（现状不动）
          USB LP 中断(CDC_Receive_FS) → usb ringbuf（新增，只写不解析——ISR 铁律不变）
主循环:   每轮轮询消费两个 ringbuf → 命令带"来源链路"上下文执行 → 应答按仲裁路由
```

**必须双 ringbuf**：现有 ringbuf 为单生产者设计，两个 ISR 抢写会竞态。各 **239B**
（`RINGBUF_SIZE = 240`，实际容量 239B —— head/tail 是 uint8_t，**上限 255，不能取 256**；
UART@9600 = 239ms 防线；USB 靠时间片兜底）。

> 🔧 **2026-09-19 订正 + 实施**：本节原写"各 256B"，但代码实为 `RINGBUF_SIZE = 128`（容量 **127B**）
> —— 实测 USB 突发命令 ≥30 条（150B）即丢应答、60 条丢一半，丢失量 ≈ (N−127)B 定量吻合
> （见 `节拍拉长与I2C总线专案.md` §5）。现按 240 兑现原始意图，并补溢出计数（§3.3）。

### 3.2 消费三机制

| 机制 | 内容 | 解决的威胁 |
|------|------|-----------|
| ① 命令级交织 | 每轮主循环轮流扫两 buf，执行完一条命令再回来读下一条（非"清空所有再执行"） | 双链路互相饿死 |
| ② 每轮时间片封顶 | 每轮命令解析预算 2ms（或每链路 ≤3 条命令），超限停手进控制节拍，积压下轮续 | 上位机猛发数据饿死 50ms 控制环 |
| ③ 发送按链路分治 | USB CDC 发送非阻塞 µs 级；UART 阻塞发送仅存在于 owner=UART 场景（低频遥控工况，够用） | 应答/遥测阻塞主循环 |

### 3.3 延迟上界与丢字节策略

- **STOP 最坏响应延迟 = 时间片 2ms + 一条命令执行时间**——毫秒级硬上界，与对端行为无关，无需特殊通道。
- 溢出丢新字节（现有行为）+ **溢出计数器**（✅ **2026-09-19 已实施**）——`RingBuffer.overflow`（中断写/主循环读，uint8_t 原子，饱和 255）+ `ringbuf_overflow()`，组装层显示于 **OLED 页 7** 的 `O<n>` 字段（异常态才出现，正常态版式不变）。丢过数据必须可见。
- 丢字节后果与恢复：文本行损坏 → 解析失败回 `?`；二进制帧缺尾 → 既有 100ms 超时重同步。
- **已知限制（既有，不在本期解决）**：单条命令内部阻塞（如 DUMP ~1.3s）时间片管不了；DUMP 属调参会话锁定期，此时另一链路无人发命令，可接受。

## 4. USB 判活与恢复感知

### 4.1 判活信号（相与）

| 信号 | 采集点 | 含义 |
|------|--------|------|
| `dev_state == USBD_STATE_CONFIGURED` | 主循环每轮查 `hUsbDeviceFS.dev_state`（µs 级） | 枚举完成（插线/PC 重新启用/驱动重绑定均触发） |
| `DTR == 1` | `CDC_Control_FS` 的 `SET_CONTROL_LINE_STATE` 捕获存静态变量 | 上位机真正打开串口 |

`usb_alive = 枚举完成 && DTR`。failover 边沿：真→假即切 UART；假→真即广播一次 `USB READY`（不抢回）。

### 4.2 设计决议（用户确认 2026-09-19）

1. **USB 恢复不自动抢回**：任何串口工具打开 COM 口都会拉 DTR，自动抢回会造成运行中指挥权跳变。恢复后仅在 owner 链路提示 `USB READY`，需 USB 侧显式 `LINK USB`。
2. **调参会话期锁仲裁**：见 §2.3。

### 4.3 供电与噪声备忘

- USB 调试会话**只让 USB 供电**，ST-LINK 保持三线（SWDIO/SWCLK/GND）。
- 接电机主电源后若 USB 间歇掉线：先查共地与走线（地弹/EMI 耦合 D+/D-），必要时磁环。观察项，登记于调试总结。
- F103 烧录软复位后 USB 不重枚举（设备描述符请求失败/问题码 43），**拔插一次 USB 即愈**——flash 流程固化步骤：烧录 → 拔插 USB → 验证枚举。

## 5. car_config.h 开关设计（✅ 已落地）

| 宏 | 默认 | =0 语义（组装层接线 + 平台层空壳替换） |
|----|------|------------------------|
| `CAR_FEATURE_USB` | 1 | usb ringbuf 不消费（消费循环整条跳过）/ 发送 no-op / `usb_alive` 恒假 / 仲裁固定走 UART / `LINK USB` 回 `LINK:OFF`——**USB 中间件代码仍在**（CubeMX 生成，12KB Flash 成本已付，编译剔除需动生成代码，不值） |
| `CAR_FEATURE_UART` | 1 | 不 init UART 中断接收 / UART 发送 no-op / **CMake 把 `uart_platform_ops.c` 换成 `uart_platform_ops_stub.c`**（同符号名 no-op）/ 仲裁固定走 USB / `LINK UART` 回 `LINK:OFF` |

开关只作用于**组装层接线 + CMake 源取舍**（组件层零 include，遵守 car_config.h 铁律）；两链路宏双 0 无意义（车失联），CMake configure 期 `FATAL_ERROR` 拒绝。

**实现分层（两层都关才算链路真正下线）**：平台 ops 空壳（驱动层）+ 组装层 `LINK_*_ENABLED` 门控（消费循环 / 两个原始发送出口 `usb_send_raw`·`uart_send_raw` / 仲裁初值 / 页面显示 / `LINK` 拒绝）。

**四组合实测（2026-09-19，`bash tools/car_cfg_matrix.sh`）**：

| USB | UART | Flash | RAM | 取舍证据 |
|:---:|:----:|-------|-----|---------|
| 1 | 1 | **55068 B / 84.03%**（交付态） | 11832 B / 57.77% | `uart_platform_ops.c` 入链；无 `LINK:OFF` 串 |
| 1 | 0 | **54076 B / 82.51%**（−992 B） | 11832 B | `uart_platform_ops_stub.c` 入链、`uart_platform_ops.c` 零出现 |
| 0 | 1 | **54440 B / 83.07%**（−628 B） | 11824 B | `uart_platform_ops.c` 入链；`LINK:OFF` 串在位 |
| 0 | 0 | — | — | **CMake 报错拒绝** ✅（预期行为） |

> 结论订正一处：原文"USB=0 不值"只针对"剔除 USB 中间件（12 KB）"——实际把 USB=0 只做接线级下线，仍能省 **628 B**（`usb_send_raw` + CDC 发送链被 gc 掉）；UART=0 省 **992 B**（阻塞式 `HAL_UART_Receive` 等随之消失）。都属于"语义收口顺带的小收益"，不是瘦身手段。

## 6. 看门狗语义升级

现有 WD（`WD <ms>`）改为**只盯 owner 链路输入**：owner 切换时喂狗基准重置；非 owner 链路的字节不喂狗。failover 本身即最快的断链响应，WD 作为 owner 断链的第二道兜底不变。

## 7. 实施分期

| 期 | 内容 | 验证 |
|----|------|------|
| P1 链路并存 ✅ | usb ringbuf + CDC 接入（去测试回显代码）+ 主循环双 buf 消费 + 应答路由（暂固定"谁的命令回谁"） | 真机：两链路同时 PING/PONG、ODOM 走 USB 20Hz |
| P2 仲裁 ✅ | link_arbiter 组件（纯逻辑，PC 桩先行）+ LINK 命令 + failover + DTR 捕获 + 会话锁 + WD 升级 | PC 桩全用例 + 真机状态机遍历（§8） |
| P3 开关收口 ✅ | car_config.h 双开关 + OLED LINK 页 + README §3 改版 + tools 探测脚本（usb_ping_test 并入统一探测） | 双宏四组合编译 + Flash/RAM 记录（§5 表）；交付态真机冒烟 |

**P3 落地清单（2026-09-19）**：

| 项 | 落点 | 状态 |
|----|------|------|
| 双开关 | `car_config.h`（+`uart_platform_ops_stub.c` 新增）+ `CMakeLists.txt`（`car_cfg_read()` 统一解析 / 双 0 拒绝 / UART stub 取舍）+ `main.c` 组装层门控 | ✅ |
| OLED LINK 页 | 页 7 `L:<owner> USB:ON/OFF U:<秒>`，异常态 `E<写失败> O<溢出> FZ`（P2 已落地，P3 只补"USB 链路编译期下线时不宣称在线"） | ✅ |
| README §3 改版 | §3.1 双链路物理层 / §3.2 路由 + **新增 §3.2.1 指挥权与 failover** / §3.3 横幅仅 UART / §3.4 补 LINK·BUSY·异步行 / §3.7 补 LINK 命令 / §3.8.1 补 USB 无带宽约束 / §3.9 区分两链路预算 / §3.10 流程与脚本表 / §3.11 新增 4 条限制 | ✅ |
| tools 探测脚本 | 新增 **`tools/link_probe.py`**（取代 `usb_ping_test.py`；USB/BT04 归类 + 逐口隔离探测 + 报 owner + 存档）；`bt_connect.py` 降为兼容壳；新增 `tools/car_cfg_matrix.sh`（四组合矩阵） | ✅ |

## 8. 测试点清单

| # | 测试项 | 前置 | 预期 | 环境 |
|---|--------|------|------|------|
| T1 | 双链路并发 PING/PONG ×50 | 双链路同开 | 100% 应答，无串扰 | 真机 |
| T2 | owner 切换三语义 | USB owner | UART 发 LINK UART 接管；UART 发 LINK USB 让出；USB 发 LINK USB 抢回；各回 OK | 真机 |
| T3 | 自动 failover | USB owner + 运行中 | 拔 USB → 串口收 `USB LOST → UART TAKEOVER`，车按 WD 停（WD 使能时） | 真机 |
| T4 | 恢复不抢回 | failover 后重插 USB | 串口收 `USB READY`，owner 仍 UART；USB 发 LINK USB 后才切 | 真机 |
| T5 | BUSY 拒绝 | UART owner | USB 发 M0 → 回 `BUSY:UART`，电机不动 | 真机 |
| T6 | 会话锁 | TEL 1 激活 | LINK 切换回 `BUSY:SESSION`；STOP 仍生效 | 真机 |
| T7 | STOP 任意链路 | 任意 owner/任意会话 | 两链路 STOP 均急停 | 真机 |
| T8 | 时间片封顶 | USB 上位机 1KB/s 猛发命令 | 控制环 50ms 节拍不漂移（示波器/遥测 tick 差值） | 真机 |
| T9 | 状态机全转移 + 边界 | — | 全部用例过 | PC 桩 |
| T10 | 溢出可见 | 制造 ringbuf 溢出 | 溢出计数器递增且可查 | PC 桩 |
| **T11（P3 新增）** | **链路开关四组合编译** | `car_config.h` 改宏 + 重新 configure | 三个有效组合零告警编译通过、双 0 被 CMake 拒绝、Flash/RAM 记录在案 | PC（`tools/car_cfg_matrix.sh`）✅ |

**执行状态**：T9 / T10 / T11 已过（PC 侧）。T1、T5 已在双链路同开场景下间接验证（USB=COM17 与 BT04=COM12 同时应答、`LINK ?` 报 owner 正确）；T3/T4（需物理拔掉 USB）与 T6/T8（需示波器/长时压力）留待真机专项，登记于开发跟踪。

## 9. 附带收益与关联待办

- **提速待办（开发跟踪 ②）改道**：ODOM 50Hz（20ms 周期）不再依赖 USART2 提速——USB 主导时直接满足，提速决策可关闭或转"仅蓝牙场景"。
- 阻塞发送类已知限制（README §3.11）在 USB owner 下自然消失；UART owner 时沿用分频应对。
