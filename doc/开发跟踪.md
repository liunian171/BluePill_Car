# BluePill_Car 开发跟踪

> 状态标注: ✅ 完成(附验证证据) / 🔄 进行中 / ⬜ 未开始 / ⚠️ 待确认·待标定
> **文档 ≠ 完成**: 只有明确标注 ✅ 才算完成；PC 验证 ≠ 硬件验证，分开标注
> 每完成一个条目立即更新本表 + 调试总结 + 本地 git commit（见 AGENTS.md §5.4）

---

## 当前阶段目标

**通过无线串口（BT04 蓝牙）可靠地向小车下发指令并获得执行确认**

链路: 发送端(手机APP/PC蓝牙) → BT04 透传(9600-8N1) → USART2 → ringbuf → 命令解析(main.c) → 执行 → 应答回传 + OLED 页6 显示最近命令

## 阶段规划

| 阶段 | 内容 | 达成标准 | 前置条件 | 状态 |
|:---:|------|---------|---------|:---:|
| W0 | 代码审查 + 修复无线链路隐患 | 编译 0 error；修复项见下表 | — | ✅ (代码审查级) |
| W1 | 测试工具 + 文档准备 | tools/bt_cmd_test.py 语法检查通过；本文档建立 | W0 | ✅ |
| W2 | 有线基线（可选） | USB-TTL 直连 PING→PONG | ⚠️ USB-TTL 是否在手 | ⬜ |
| W3 | 无线链路验证 | 配对成功→PING→PONG；OLED 页6 显示 CMD:PING->PONG | 发送端就绪 | ⬜ |
| W4 | 无线电机指令验证 | --auto 用例全 PASS + 时延记录 | W3 | ✅ 13/13 PASS，时延 30~63ms（2026-09-09，PC COM14 直驱；未接电机，轮子运动验证归 W6） |
| W5 | 无线健壮性验证 | --bin 用例：非法 id 不越界、截断帧后 100ms 重同步恢复 | W4 | ✅ 6/6 PASS（非法 id 回 `?`、截断帧丢弃后 PONG 恢复 94ms） |
| W6 | 巡线联动 + 安全停车 | 运动中 L 0 / STOP 任意时刻可停；GK/GS 在线调参生效 | W4 + 实车场地 | ✅ 实车验证：L 1/0、STOP（不复活）、M0/M1 30 全过（2026-09-09） |
| W7 | 文档闭环 + commit | 调试总结补 W3-W6 实测数据；git commit | W3-W6 | ⬜ |

> **W2-W6 属硬件在环，按 AGENTS.md 两级模式执行**：一级 Agent 直驱烧录+串口判读；二级给指令块由用户执行回读。
> 发送端两种方案：① 手机蓝牙串口 APP（手动发）；② PC 配对 BT04 出 SPP COM 口 → 跑 `tools/bt_cmd_test.py`（自动判读，优先）。

## 条目状态表

| 条目 | 内容 | 验证证据 | 状态 |
|------|------|---------|:---:|
| W0-1 | 文本命令 `\r` 过滤：手机 APP 发 `\r\n` 行尾时 `"PING\r"` 导致 strcmp 匹配失败 | 代码修复 + 编译通过（无真机验证） | 🔄 |
| W0-2 | 二进制帧 id 越界保护：`spd_target[id]` 等以未校验的帧内 id 索引，非法 id(≥2) 越界写内存 | 0x01/0x02/0x03/0xE0 均加 `frame[2] < 2` | 🔄 |
| W0-3 | 0 速必停语义：PID 循环原 `target<1 → continue` 使 PWM 保持旧值，`M0 0` 不停车 | target<1 时 pid_reset + motor_bridge_brake | 🔄 |
| W0-4 | Flash 超限预警：Debug(-O0) 编译 65120B / 99.37% | flash.bat 切换 Release 构建，42704B / 65.16% | ✅ |
| W1-1 | 上位机测试脚本 tools/bt_cmd_test.py（--ping/--auto/--bin/交互 4 模式，含时延统计） | py_compile 语法检查通过 | ✅ |
| W1-2 | 开发跟踪文档建立（本文件） | — | ✅ |
| W1-3 | OLED 页4 改为 RSP 页：显示最近发回手机的应答（原 DIR 方向行移除）；寻迹诊断事件不占用应答页 | 代码修改 + Release 编译通过 + 已烧录 | 🔄 |
| W3-1 | 无线链路心跳/急停/刹车验证 | 真机实测：PING→PONG、STOP→STOP OK、B0/B1→BRAKE 均正常 | ✅ |
| W3-2 | `M0 60` 无响应根因修复：newlib-nano 缺 `_scanf_float`，sscanf %f 静默失败 | 根因分析 + txt_cmd 模块化 + PC 测试桩 41/41 PASS + 烧录；**真机回归通过**（M0/M1/0速/GK/GS 全过，2026-09-09） | ✅ |

## 当前进行中

- ✅ **W6 复测通过**（L 1/0、STOP、M0/M1 30 实车全过），**无线指令阶段（W0-W6）关闭**
- **✅ 舵机转向阶段（SS1-SS3）完成**：无线遥控全链路验证通过（后轮控速 + 前轮转向 + 急停），2026-09-09 实车确认"能正常遥控"
- 收尾：本地 git commit（无线阶段 + 舵机阶段改动 + 文档均未提交）

## 决策记录（2026-09-09 用户确认）

1. 发送端 = 手机蓝牙串口 APP（手动发指令，看 OLED/应答判读）
2. BT04 = 经典蓝牙 SPP；USB-TTL 在手（W2 有线基线可用作链路问题隔离）
3. OLED 页4 由 DIR 方向显示改为 RSP 最近应答显示（方向信息弱化，页1 仍有目标/实际转速）

## 待确认信息 ⚠️

1. ~~BT04 模块类型~~ → ✅ 经典 SPP
2. ~~发送端方案~~ → ✅ 手机蓝牙 APP
3. ~~USB-TTL~~ → ✅ 在手
4. **BT04 透传波特率**是否为 9600（固件已按 9600 配置；若模块配过 115200 需 AT 改回或改固件）——W3 第一次 PING 无应答时优先排查此项

## 已知限制（不阻塞当前目标）

- 二进制帧参数字节中若恰好出现 `FF FF` 会提前截帧（float RPM 参数实际不会产生，低风险）
- 应答帧为文本格式（`M0:60RPM`），与 uart_cmd_parser.h 定义的二进制响应帧未统一——后续上位机协议化时再统一
