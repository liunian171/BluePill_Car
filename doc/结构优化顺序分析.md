# 结构优化顺序分析（契约先行，实现后搬）

> 创建: 2026-09-10，基于《架构评审文档.md》C1-C8 发现的治理顺序推演。  
> **当前阶段定位：学习 + 思考期。** 本文档仅记录分析结论与判断框架，供后续提问讨论用；  
> 不启动任何优化实施——实施动作待用户思考完成、确认方案后另行立项。  
> 关联: `doc/架构评审文档.md`（发现清单与严重度评级）



---

## 0. 核心结论

**从上层开始，但把改动分成两类，顺序不同：**

- **接口 / 契约类改动**（改签名、改依赖方式）→ **先做，自下而上**
- **实现 / 搬迁类改动**（搬代码、收编逻辑）→ **后做，自上而下**

一句话原则：**先定契约，再搬实现**。上层组件必须建在最终接口上，否则接口一变、上层返工两次。

---

## 1. 为什么整体从上层开始

|  #  | 理由     | 说明                                                                      |
| :-: | ------ | ----------------------------------------------------------------------- |
|  1  | 依赖传播方向 | 改上层 = "消费"下层，编译器立即兜底；改下层 = 级联到所有上层调用点，破坏面大                              |
|  2  | 痛点位置   | 高严重度 2 项中 C1 纯上层；C2 的收益（可 PC 测试）服务于上层巡线融合；main.c 本身就是耦合热点               |
|  3  | 下层已经健康 | pwm/uart/gpio/encoder 五套 ops 驱动是全库质量最好的部分，且已实车验证——动它要重新走硬件在环回归，成本最高收益最小 |
|  4  | 回归手段   | 上层改动可用"编译 + 串口命令回归 + PC 测试桩"闭环验证；下层改动只能上实车                              |

> 直觉上"底层是地基所以先改底层"在本项目不成立：地基没裂，裂的是地基上面的施工层。

## 2. 两类改动的划分与顺序

| 类别     | 典型项                                                                            | 特征                  |  顺序 |
| ------ | ------------------------------------------------------------------------------ | ------------------- | :-: |
| 接口/契约类 | C2 传感器注入（line_follower 加读取接口）、C5 OLED 句柄注入（init 加 I2C_Handle 参数）、C7 init 加方向参数 | 改的是"调用约定"，上层绑定点要跟着动 |  先  |
| 实现/搬迁类 | C1 收编协议分发、C3 抽 steering / speed_loop 组件、C4 仲裁机制、main.c 减肥                      | 不改下层接口，只动实现位置       |  后  |

关键事实：C2/C5 这类接口改动**只影响 main.c 的接线行（init 调用），不影响业务逻辑**。先做它们，后续上层搬迁只改几行绑定代码，返工成本极低。

## 3. 执行序列（结合评审文档 §4.3）

|  步  | 动作                                      |    层   | 位置理由                              |
| :-: | --------------------------------------- | :----: | --------------------------------- |
|  1  | C1 删除/收编 uart_cmd_parser                |   上层   | 纯减法、零接口变更、无下层依赖；顺带清 Flash         |
|  2  | C2 传感器注入 + C5 OLED 注入                   | 中/下层接口 | 契约修正：后续所有上层组件建在可注入接口上，避免二次返工      |
|  3  | C3 抽 steering / speed_loop 组件 + C4 仲裁机制 |   上层   | 消费第 2 步定好的契约；组件按 txt_cmd 范本建 PC 桩 |
|  4  | C6/C7/C8 + D1-D3 低级别项                   |  下层/文档 | 穿插随手修，不单独排期                       |
|  5  | car_config 表、参数持久化等机制                   |   横切   | 配合 T5 一起做收益最大                     |

## 4. 判断口诀（可复用的决策框架）

以后每次纠结先后，问两个问题：

1. **这个改动会不会改下层接口？** 会 → 属于"契约"，排在使用它的上层重构**之前**
2. **改完怎么验证？** 只能上实车 → 尽量少动、攒批验证；能 PC/串口回归 → 可以大胆先动

## 5. 风险提醒

C2 动的是 line_follower（巡线唯一权威模块）：改完必须先用现有文本命令（L 1 / GK / GS）在台架回归，再上实车；**不与 C1/C3 改动混在同一次烧录里**——一次烧录只验证一件事。

---

## 6. 阶段声明（2026-09-10 用户确认）

- 下一阶段为**用户学习 + 思考期**：围绕本文档与评审文档的结论提问、追问、验证理解
- 本阶段**不执行**任何优化改动；C1-C8 维持现状
- 思考成熟后再决定实施范围与顺序，届时按 AGENTS.md §5.4 闭环流程立项

## 7. 待设计议题登记

| 议题                                     | 登记时间                         |      状态      | 说明                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| -------------------------------------- | ---------------------------- | :----------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **桥接层家族契约设计**                          | 2026-09-10（用户："之后再对桥接层进行设计"） |    🔄 思考中    | 学习思考期之后的专项设计。设计输入：代码风格与模块衔接指南 §1（🔶 bridge 家族契约）/ §3.3（静态池规则）/ §4.2 义务 6（越界+静默失败禁令）；评审 C7/C5/S8。待决点：① init 签名统一（id, config 结构?）② 错误通道形态（返回码 vs 日志 vs 两者）③ id 容量与静态池模板化 ④ oled_bridge 无 id 单例的归一 ⑤ 与 C2/C5 契约类改动的先后衔接                                                                                                                                                                                                                                      |
| **IMU/姿态链重设计**（上项的子议题，2026-09-11 分析确认） | 2026-09-11                   |  ⬜ 待与舵机对照后汇总 | 已确认的思路：① 平行数组聚合为"通道实例"结构体（device/type/ready/filter/last_tick + 校准状态），实例数组随姿态组件走，顺带修复 cal_progress 忽略 id 的单例假设 bug；② MPU6050 = 器件实例（应改静态池，禁堆 new）；③ 校准状态机+漂移补偿+Mahony 调度（现 imu_bridge 内 ~90 行）下沉为"姿态组件"=业务组件层（同 line_follower 性质，IIMU 注入 + tick 外部化，消 HAL_GetTick 直调）；④ ImuFilter 留在 common 不动；⑤ imu_bridge 回归薄门面。与舵机命令域状态机（main.c sv_lim/servo_apply）为对称问题，两者分析齐后出家族契约完整方案                                                                                |
| **感知/决策双栈模型 + 按功能裁剪构建**                | 2026-09-11（用户提出）             | ⬜ 待舵机问题解决后启动 | 用户洞察：姿态是业务层的硬件隔离器，使业务层近乎纯软件。确认的双栈模型：业务层下方两条镜像链——感知栈（器件→感知组件→物理量上行：姿态/巡线感知/里程计）与执行栈（意图→功能对象→器件→ops 下行）；巡线状态机只认物理量、不持有任何实例，实例拼接与接管权仲裁归 main 组装层。line_follower 现内部混三件事（灰度读取 C2 / 位置解算 / 决策），拆出"巡线感知组件"与姿态组件同构。按功能裁剪构建：前提=接口隔离完成；机制=链接器 GC 已启用（gcc-arm-none-eabi.cmake:29,45，死代码已自动剔除）+ CMake 源文件开关 + 集中特性宏（并入 car_config.h）；注意 GC 按调用图不按功能，功能级裁剪必须显式特性开关；约束=开关控制在整车可选配置级（4~5 个），每开关独立测试点清单；收益=OLED 字库/IMU 链/舵机链可整体裁剪（需 map 实测）。产出物：特性表（功能→依赖链→裁剪文件→省多少）作为施工图 |
| **上层命名审查结论**（双栈模型的连带产出，2026-09-11）     | 2026-09-11                   |   ✅ 已定稿并落盘   | 已回写：代码风格与模块衔接指南 §1（双栈总览 + 5 类数据链 + 10 层定义表 + 1.3 旧命名问题五条）+ AGENTS.md 4.1（分层图按新命名重写，含旧名对照与数据方向说明）。定稿命名：组装层（旧应用层）/ 桥接层（C 门面）/ 决策组件 / 感知组件 / 执行组件 / 执行对象层（旧功能对象）/ 器件层（补名，旧"业务对象"部分）/ 驱动策略层·平台层 / 通用算法层（common）。代码目录迁移随实施期；工程文档.md 架构节（文件清单）随代码迁移同步                                                                                                                                                                                                           |

---

## 8. 思考期进度与差距评估（2026-09-11 更新）

### 8.1 本轮学习思考期已确认的结论（按时间序）

1. **桥接层机制与现状**：静态池/placement new/薄委托/id 索引/enum+void* 五决策吃透；四桥盘点——servo 最标准、motor 模式对契约缺、imu（堆 new+胖桥+平行数组）与 oled（无 id 单例）偏离模式
2. **IMU/姿态链方案**：通道实例结构体（含校准状态入库，修 cal_progress 忽略 id bug）、MPU6050 改静态池、~90 行校准管线下沉为姿态组件、imu_bridge 回归薄门面
3. **双栈模型**（用户关键洞察：姿态是业务层的硬件隔离器）：感知栈（器件→感知组件→物理量上行）与执行栈（执行组件→执行对象→器件→ops 下行）镜像并列，决策组件居中，组装层汇聚；全库 5 类 8 条数据链
4. **决策组件接口面板**（交界不拥有）：仅 4 个接触面——感知入、意图出、tick 入、使能入；检验标准=删光实现只留头文件，决策代码一行不改
5. **上层命名定稿**：组装/桥接/决策/感知/执行组件/执行对象/器件/驱动策略·平台/通用算法，已回写指南 §1 与 AGENTS.md 4.1
6. **按功能裁剪构建**：前提=接口隔离；GC 已启用但只按调用图；功能级需特性宏+CMake 开关，开关限 4~5 个整车级

### 8.2 接下来要做的事（既定顺序）

> **优先级决策（2026-09-11 用户确认）**：**先补全执行组件（steering）**，`main.c` 只保留"最小必要接线"（init + 命令转发 + 应答）；  
> **组装层的其余治理（main 减肥、接管权仲裁机制化 C4）等执行组件跑通之后再改**。  
> 即实施期第一步产出物 = `steering` 执行组件（含 PC 桩 + 测试点清单），而非先动 main 结构。

|  序  | 事项                                                                                   | 负责              |                                                                              状态                                                                             |
| :-: | ------------------------------------------------------------------------------------ | --------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------: |
|  1  | **舵机侧对照分析**（用户）：用三问拆 main.c 的 sv_lim/servo_apply——属于哪层/实例在哪/与 attitude 是否同构          | 用户思考 + AI 协助    | ✅ 完成（2026-09-11 → `doc/舵机代码结构对照分析.md`）：舵机命令域判定为**执行组件（转向）**；与姿态同构（都是标定/管线状态长错位置）但**执行栈单通道不需数组化**；11 项偏差 S1-S11 + 钳位两类两层结论；S1 已剥离归档（`doc/舵机旧设计稿_归档_上游A板.md`） |
|  2  | **转向执行组件（steering）契约 + 实现**（新优先级第一位）                                                 | AI 起草 + 实现，用户确认 |                                             🔄 **当前主关注**：待定 2 项契约输入（域边界 / 机构限位所有者）→ 出契约（含单位/域声明、PC 桩、测试点清单）→ 实现                                             |
|  3  | 汇总**桥接层家族契约 + 组件拆分完整方案**（IMU 链 + 转向 + 命令链收编一份设计文档）                                   | AI 起草，用户确认      |                                                                         ⬜ 与序 2 并行推进                                                                         |
|  4  | A 桶文档回写（开发跟踪 W0-1~3/W7、调试总结 §11/§12 证据、AGENTS §七超时重置勾选、txt_cmd.h/line_follower.h 注释） | AI 可随时插入        |                                                             ✅ 基本完成（2026-09-11 已回写）；余 S9/S11 注释小项                                                            |
|  5  | 组装层治理：main 减肥 + 接管权仲裁机制化（C4）——**等执行组件跑通后**                                           | 按 §5.4 闭环       |                                                                        ⬜ 等 2/3（用户已排后）                                                                       |
|  6  | 实施期其余：契约先行（C2/C5 接口）→ 实现后搬（C1/C3）                                                    | 按 §5.4 闭环       |                                                                            ⬜ 等 3                                                                            |
|  7  | car_config.h + 特性表 + 按功能裁剪                                                           | 配合 T5           |                                                                            ⬜ 等 6                                                                            |

### 8.3 离"好系统"还有多远

**好系统画像**（本项目的目标态）：双栈组件各就各位、main 只做组装与仲裁、命令链单一、核心组件 PC 桩全测、配置集中可裁剪、文档与代码一致。

| 维度         | 现状                            | 差距（剩余工作）                                  |  完成度 |
| ---------- | ----------------------------- | ----------------------------------------- | :--: |
| 驱动层（策略+平台） | 5 套 ops 健康，实车验证               | pwm.h include 下沉、encoder init 风格统一（C6/小项） | ~90% |
| 资源纪律       | placement new+静态池成熟、Flash 66% | imu_bridge 堆 new 改造、firewater 死码清理        | ~85% |
| 文档体系       | 评审/指南/顺序分析三层齐备，命名刚定稿          | A 桶状态回写悬项                                 | ~70% |
| 桥接层        | 2/4 合格                        | 家族契约设计 + imu 改造（堆 new/胖桥/平行数组）+ oled 归一   | ~50% |
| 组件层        | txt_cmd 范本 + line_follower 雏形 | 拆巡线感知、建姿态/速度环/转向组件（T1/T4 前置）              | ~35% |
| 组装层        | 节拍调度正确                        | main 减肥 + 接管权仲裁机制化（C3/C4）                 | ~30% |
| 测试体系       | txt_cmd 41 用例 + PC 桩方法论       | 3~4 个组件补桩 + 测试点清单制度化（§5.3 仅 1.5/5）        | ~25% |
| 配置/裁剪      | 链接器 GC 已启用                    | car_config 表、特性开关、裁剪施工图                   | ~15% |

**总体判断**：**骨架约 70%，肌肉约 40%，神经约 25%——综合走了一半略强。**

- 已经达标、不需要再动的：驱动层、静态池纪律、踩坑闭环机制、txt_cmd 范本
- 剩余距离的性质：**大头不是写新代码，而是"搬迁+接线"**——把已经实车验证过的逻辑（舵机命令域、速度环、校准管线）搬进正确的层，风险低但必须按"契约先行→实现后搬"顺序走，避免二次返工
- 最短路径提示：契约类改动（C2/C5/家族契约签名）是所有后续搬迁的地基，先行完成；A 桶文档回写零风险可随时插入
