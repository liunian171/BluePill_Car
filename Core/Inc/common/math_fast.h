/**
 * ============================================================================
 *  math_fast.h — 紧凑 sinf/cosf 声明（实现说明见 math_fast.c）
 * ============================================================================
 *
 *  用法：**调用方无需改动** —— 本工程定义强符号 `sinf`/`cosf`，
 *  链接器优先采用，newlib 的全量三角实现（含 4.1KB 范围规约链）不再被拉入。
 *
 *  ⚠️ 精度等级（引入新数学函数前必读）：
 *     |x| ≤ ~1e5 rad 内与 newlib 差 < 1e-6；本车调用域（≤2π）差 < 1e-7。
 *     若将来需要 1ULP 级精度或其它 libm 全范围函数，需重新评估本替代是否成立。
 * ============================================================================
 */

#ifndef MATH_FAST_H
#define MATH_FAST_H

#ifdef __cplusplus
extern "C" {
#endif

/* 显式声明（与 <math.h> 签名一致，可共存）——供需要显式调用的场合 */
float sinf(float x);
float cosf(float x);

#ifdef __cplusplus
}
#endif

#endif /* MATH_FAST_H */
